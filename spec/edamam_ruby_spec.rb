require "spec_helper"

describe EdamamRuby do
  it "has a version number" do
    expect(EdamamRuby::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
